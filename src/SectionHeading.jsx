import React from 'react'

export default function SectionHeading({heading}) {
  return (
    <div>
        <h1 className='text-3xl text-slate-700 font-semibold font-mono m-5'>{heading}</h1>
    </div>
  )
}
